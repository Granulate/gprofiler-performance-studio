

import { Typography } from '@mui/material';

const HostnameDescription = () => {
    return <Typography variant='body3_lato'>Hostname of the node from which profiles were collected</Typography>;
};

export default HostnameDescription;
